apiVersion: v1
kind: Service
metadata:
  name: physml
  labels:
    app: physml
spec:
  selector:
    app: physml
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP

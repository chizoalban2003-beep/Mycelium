# PhysML — core dependencies
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.12,<2

# Prediction core (required)
scikit-learn>=1.4,<2

# Optional: richer outlier handling
feature-engine>=1.7,<2

# LLM backbone (optional — install with pip install 'physml[llm]')
# anthropic>=0.40
